name: question_answering

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      system_prompt: {type: str, default: "Your job is to answer questions about MLflow."}
    command: "python question_answering.py '{system_prompt}'"